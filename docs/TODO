-- IMMEDIATE
- Fix issue with login persistence (we did rebuild the db every time though.)
- Create a Search API
- Create API for Profile stats
- Deal with Orphaned screens (BubbleDetail, UserProfileView)
- Wire Settings properly
- Add notification toggles!
- topic buttons don't work in FeedView
- make GraphFeedView more bubbly
- Fix issue where only tech shows up.

-- BEFORE PRODUCTION
add media support

-- IMPORTANT CHECKS
routing efficiency/gotchas

-- FUTURE
add proper topic system (e.g. AI/ML based topic determination)
   -- NOTE: Continue from step 3.2