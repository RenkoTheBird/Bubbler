<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug: allow cleartext to the local backend (emulator 10.0.2.2 / LAN IP). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
